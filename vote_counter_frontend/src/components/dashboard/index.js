import React from 'react';

const Dashboard = () => {
  /**Sample API call with api folder
   * useEffect(() => {
        createAPIEndpoint(ENDPOINTS.LOGIN).fetchAll()
            .then(res => {
                //code here
            })
            .catch(err => console.log(err))

    }, [])
   * 
  */

  return <div>Dashboard</div>;
};

export default Dashboard;
